import { NodeCorePluginClient } from "../../core/client";
import { DPayloadLike } from "../../datatypes/common";

export default function handleFileCommand(client: NodeCorePluginClient, payload: DPayloadLike[]) {
    
}