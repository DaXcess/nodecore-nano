import { NodeCorePluginClient } from "../../core/client";
import { PayloadLike } from "../../datatypes/common";

export default function handleProcessCommand(client: NodeCorePluginClient, payload: PayloadLike[]) {
    
}